import './index.css'

export default function BackgroundRadialDrop({ radial_class }) {
    return (
        <div className={`bg-hero_highlight-dark rounded-full radial_class ${radial_class}`}>
        </div>
    )
}